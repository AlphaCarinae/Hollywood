
<h2><%= @list.name %></h2>

<ul>
  <% @list.categories.each do |category| %>
  <li>
      <%= link_to "#{category.member.title}", "/movies/#{category.member.id}"  %>
      <%= button_to "delete", {:controller => :lists, :action => 'removeitem', :id => @list.id, :movie_id => category.member.id }, :method => :delete %>

  </li>
  <% end %>
</ul>

<%= link_to "Edit details", edit_list_path(@list) %>
<%= link_to "Delete List", edit_list_path(@list) %>
