<h1>Users#index</h1>
<ul>
<% @users.each do |user| %>
  <li>
    <%= user.email %> :
    <%= check_box_tag "admin", "#{user.admin}" %>
    <%= check_box_tag "moderator", "#{user.moderator}" %>
    <%= link_to edit_user_path(user) %>
  </li>
<% end %>
</ul>
